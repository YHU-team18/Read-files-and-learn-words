#!bin/bash

# docker compose up -d
# docker compose exec db2 /bin/bash
# psql -h localhost -U db_user -d postgres
# ^D
# cd docker-entrypoint-initdb.d/
# head -n5 word_meaning.sql 
# \dt public.* で確認をしても myTableが追加されていない
